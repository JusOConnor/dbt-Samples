# Copy this file to .env and adjust the values if yours differ.
# The real .env is ignored by git, so your credentials stay private.
# These same values feed both Docker Compose and dbt.

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=dbt_lab
POSTGRES_USER=dbt_user
POSTGRES_PASSWORD=dbt_password
